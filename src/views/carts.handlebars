
<table id="cartContentTable" class="table">
    <thead>
        <tr>
            <td></td>
            <td><strong>Product</strong></td>
            <td><strong>Price</strong></td>
            <td></td>
        </tr>
    </thead>
    <tbody>
        {{#each products}}
        <td><img src="{{product.thumbnail}}" style="width: 5rem;" alt="no image"></td>
            <td>{{product.title}}</td>
            <td>{{product.price}}</td>
            <td>
                <button class="btn btn-danger delete-btn" id="deleteBtn_{{product._id}}">Delete</button>
            </td>
        {{/each}}
    </tbody>
</table>

